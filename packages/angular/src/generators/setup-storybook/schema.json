{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "SetupStorybook",
  "title": "",
  "type": "object",
  "properties": {
    "appName": {
      "type": "string",
      "description": "The name of the application to add Storybook for.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What app would you like to add Storybook for?",
      "x-dropdown": "projects"
    }
  },
  "required": ["appName"]
}
